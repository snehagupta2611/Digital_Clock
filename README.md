# ⏰ Digital Clock Project

A simple, real-time local digital clock built with HTML, CSS, and JavaScript.

# 📂 Files

- **index.html**: Defines the clock structure.
- **style.css**: Centers the content and applies dark theme with a bright cyan clock face.
- **script.js**: Uses setInterval(..., 1000) to continuously fetch the current time (new Date().toLocaleTimeString()) and update the #clock display every second.

# 🚀 Usage

1. Save the three files (index.html, style.css, script.js) in the same folder.
2. Open index.html in any web browser.
